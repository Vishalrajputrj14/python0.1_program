# If ... Else in one line 

'''
THere is also a shorthand syntax for the if-else statement that can be used when the condition being tested is simple and the code blocks 
to be exeuted are short. Here an example:
'''

a = 20000
b = 33.000000
print("A") if a > b else print("=") if a == b else print("b")
